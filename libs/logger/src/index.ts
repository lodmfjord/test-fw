/**
 * @fileoverview Implements index.
 */
export { createLogger } from "./create-logger";
export { createNoopLogger } from "./create-noop-logger";
export type { CreateLoggerInput, Logger, LoggerPayload } from "./types";
