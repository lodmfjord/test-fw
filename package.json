{
  "name": "babbstack",
  "private": true,
  "packageManager": "bun@1.3.0",
  "type": "module",
  "engines": {
    "bun": "1.3.0"
  },
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "scripts": {
    "dev": "bun run --cwd apps/test-app dev",
    "dev:client": "bun run --cwd apps/test-app-client dev",
    "dev:all": "sh -c 'bun run --cwd apps/test-app dev & server=$!; trap \"kill $server\" EXIT INT TERM; ready=0; for _ in $(seq 1 60); do if curl -fsS http://localhost:3000/env-demo >/dev/null 2>&1; then ready=1; break; fi; sleep 1; done; if [ \"$ready\" -ne 1 ]; then echo \"test-app did not become ready on http://localhost:3000\"; exit 1; fi; bun run --cwd apps/test-app-client dev ${1:-http://localhost:3000}' --",
    "build:libs": "bun run --cwd libs/schema build && bun run --cwd libs/step-functions build && bun run --cwd libs/dynamodb build && bun run --cwd libs/sqs build && bun run --cwd libs/s3 build && bun run --cwd libs/http-api-contract build && bun run --cwd libs/client build && bun run --cwd libs/create-app-cli build",
    "build": "bun run build:libs",
    "test": "bun run build:libs && bun test",
    "test:watch": "bun test --watch",
    "tdd": "bun run test:watch",
    "typecheck": "bun run build:libs && tsc --noEmit",
    "lint": "biome lint .",
    "lint:fix": "biome lint . --write",
    "format": "biome format . --write",
    "format:check": "biome format .",
    "fix": "bun run format && bun run lint:fix",
    "check:constraints": "bun run tools/check-constraints.ts",
    "check:fast": "bun run check:constraints && bun run format:check && bun run lint && bun run typecheck",
    "check": "bun run check:constraints && bun run format:check && bun run lint && bun run typecheck && bun run test",
    "create:app": "bun run --cwd libs/create-app-cli build && bun libs/create-app-cli/dist/create-app-bin.js"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.14",
    "@types/bun": "1.3.9",
    "typescript": "5.9.3"
  }
}
