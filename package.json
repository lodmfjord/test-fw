{
  "name": "babbstack",
  "private": true,
  "packageManager": "bun@1.3.0",
  "type": "module",
  "engines": {
    "bun": "1.3.0"
  },
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "scripts": {
    "dev": "bun run --cwd apps/test-app dev",
    "build:libs": "bun run --cwd libs/schema build && bun run --cwd libs/step-functions build && bun run --cwd libs/dynamodb build && bun run --cwd libs/sqs build && bun run --cwd libs/s3 build && bun run --cwd libs/http-api-contract build",
    "build": "bun run build:libs",
    "test": "bun run build:libs && bun test",
    "test:watch": "bun test --watch",
    "tdd": "bun run test:watch",
    "typecheck": "bun run build:libs && tsc --noEmit",
    "lint": "biome lint .",
    "lint:fix": "biome lint . --write",
    "format": "biome format . --write",
    "format:check": "biome format .",
    "fix": "bun run format && bun run lint:fix",
    "check:constraints": "bun run tools/check-constraints.ts",
    "check": "bun run format:check && bun run lint && bun run typecheck && bun run test && bun run check:constraints"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.14",
    "@types/bun": "1.3.9",
    "typescript": "5.9.3"
  }
}
